description: reproducible_check failure after apply reverts the change and journals rejection
steps:
  - run: [init]
  - run: [propose, --layer, context, --target, AGENTS.md, --patch, fix.patch,
          --rationale, "Repo uses pnpm.", --producer, claude-code, --id, cs-20260830-c014]
  - run: [evidence, cs-20260830-c014, --record, ev-transcript.json, --artifact, transcript-fail.json]
  - run: [gate, cs-20260830-c014]
  - run: [apply, cs-20260830-c014]
    expect_exit: 1
  - expect_file: { path: AGENTS.md, equals: "# Agent notes\n\nUse npm install to set up.\n" }
expected_events: [genesis, proposed, gated, rejected]
