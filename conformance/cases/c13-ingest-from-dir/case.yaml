description: a prepared ChangeSet folder from an independent Producer ingests, gates, applies (D2 portability)
steps:
  - run: [init]
  - run: [propose, --from-dir, "{case}/changeset"]
  - run: [gate, cs-20260830-c013]
  - run: [apply, cs-20260830-c013]
  - expect_file: { path: AGENTS.md, contains: pnpm }
expected_events: [genesis, proposed, gated, applied]
