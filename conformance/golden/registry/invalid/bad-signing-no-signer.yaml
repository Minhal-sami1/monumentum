# REQUIRED-FAILURE: signing block requires both key_file and signer.
spec: monumentum/v0.1
kind: git-remote
remote:
  url: https://example.internal/acme/monumentum-registry.git
signing:
  key_file: keys/producer-a.key
