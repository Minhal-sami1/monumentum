# REQUIRED-FAILURE: L9 is not a trust level.
spec: loop/v0.1
envelope:
  context:
    level: L9
    gates: [evidence_required]
    targets_allow: ["AGENTS.md"]
  capability:
    level: L1
    gates: [evidence_required, human_review]
    targets_allow: ["tools/**"]
  architecture:
    level: L1
    gates: [evidence_required, human_review]
    targets_allow: ["agents.yaml"]
protected: [".loop/**"]
