{
  "check_cmd": "grep -q 'LF line endings' AGENTS.md",
  "runs": [
    { "phase": "before", "cmd": "grep -q 'LF line endings' AGENTS.md", "exit_code": 1 },
    { "phase": "after", "cmd": "grep -q 'LF line endings' AGENTS.md", "exit_code": 0 }
  ]
}
